export interface UserInterface{
    id: number;
    name: string;
    date: string; // Consider changing this to a Date object if you are handling date objects.
    email: string;
    password: string;
}